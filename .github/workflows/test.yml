name: Tests

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]

  push:
    branches: [main]

  release:
    types: [published]

jobs:
  test:
    name: PHP ${{ matrix.php }}
    runs-on: ubuntu-latest

    strategy:
      matrix:
        php: ['7.4', '8.0', '8.1', '8.2', '8.3']

    steps:
      - name: ✨ Checkout repository
        uses: actions/checkout@v3

      - name: ⚙ Setup PHP ${{ matrix.php }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, pdo, sqlite, xdebug
          coverage: xdebug
          tools: composer:v2

      - name: 🔄 Install Composer dependencies
        run: composer install --prefer-dist --no-interaction

      - name: 🔢 Validate composer configuration
        run: composer validate --strict

      - name: 🔧 Run unit tests (PHPUnit with coverage)
        run: vendor/bin/phpunit --coverage-clover=coverage.clover
        env:
          XDEBUG_MODE: coverage

      - name: 📊 Run static analysis (PHPStan)
        run: vendor/bin/phpstan analyse --memory-limit=512M

      - name: 🧹 Debug Show coverage file
        if: always()
        run: |
          echo "Checking if coverage.clover exists:" 
          ls -lh coverage.clover || echo "❌ coverage.clover not found"

      - name: 📄 Upload coverage to Coveralls
        if: always()
        run: vendor/bin/php-coveralls --coverage_clover=coverage.clover --json_path=coveralls-upload.json -v
        env:
          COVERALLS_REPO_TOKEN: ${{ secrets.COVERALLS_REPO_TOKEN }}
